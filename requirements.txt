# Pinned 2026-06. To upgrade deliberately:
#   1. Bump ONE package version here.
#   2. pip install -r requirements.txt
#   3. Run `pytest` and smoke-test the app locally.
#   4. Commit the bump on its own.
Flask==3.1.3
gunicorn==26.0.0
spotipy==2.26.0
python-dotenv==1.2.2
flask-cors==6.0.2
requests==2.33.1
pycountry==26.2.16
